name: greensteps
description: A new Flutter project.
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # UI & Utilities
  cupertino_icons: any
  provider: any
  shared_preferences: any
  google_fonts: any
  fl_chart: any

  # Firebase (auto-sync versions)
  firebase_core: any
  firebase_auth: any
  cloud_firestore: any
  firebase_storage: any

  # fl_chart: ^0.66.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: any

flutter:
  uses-material-design: true

  assets:
    - assets/images/
